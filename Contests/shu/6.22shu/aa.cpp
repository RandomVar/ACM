#include<bits/stdc++.h>
#define ll long long
using namespace std;
const int maxn=50010;
ll dp[maxn][5100];
ll ans[maxn];
int k;

int main(){
   int n;
   while(cin>>n>>k){
      /*for(int i=1;i<n;i++){
        int u,v;
        cin>>u>>v;
        edge[u].push_back(v);
        edge[v].push_back(u);
      }*/
     ll  cnt=0;
      //dfs1(1,0);
      //dfs2(1,0);
      cout<<cnt<<endl;
   }
   return 0;
}
